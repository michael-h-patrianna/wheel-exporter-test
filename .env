# Project-wide environment defaults
# Adjust values locally if needed. Automation will inherit these unless overridden.

# Ensure Vite and runtime tooling operate in local-development mode
NODE_ENV=development
# (Leave CI undefined locally; automation environments should export CI=1)

# Default app metadata used by Vite during build and preview
VITE_APP_TITLE=Wheel Exporter Test

# Prevent Playwright from keeping the HTML report server running after tests finish
PLAYWRIGHT_HTML_OPEN=never

# Disallow interactive Vitest watch unless explicitly enabled per instructions
ALLOW_VITEST_WATCH=0
